
@*@using LegendCR.CommonDefinitions.DTO;
@using LegendCR.CommonDefinitions.DTO;
@using LegendCR.Helpers;
@model List<PickUpRequestRecord>
@if (Model != null && Model.Count > 0)
{
    foreach (var item in Model)
    {
        <tr>
            <td><input type="checkbox"></td>
            <td>@item.PickUpRequestId</td>
            <td>@item.VendorRefName</td>
            <td>@item.VendorAddress <br />@item.VendorRefPhone</td>
            <td>@(item.ShipmentCount ?? 0)</td>
            <td>@(item.PickupDate.HasValue ? item.PickupDate.Value.ToShortDateString() : "---")</td>
            <partial name="_StatusPickupSwitch" model="@item" />
            <td>@(item.OrderDescription ?? "---")</td>
        </tr>
    }
}
else
{
    <tr>
        <td colspan="16" class="text-danger text-center"><h3>No data available in table</h3></td>
    </tr>
}*@